// TODO: I think numeric values for each enum value should be hardcoded (so it matches teh DB)
//       or do the DB values come from here??? I don't know.
export enum ArgType {
  NUMBER,
  STRING,
  BOOLEAN,
  INVALID,
}
